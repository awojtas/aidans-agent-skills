# Visual-repro harness config. Copy to <webapp>/.env.e2e (gitignored) and fill in.
# TEST credentials only — never a real user's account.

REPRO_BASE_URL=https://your-app.example.com
REPRO_EMAIL=test-account@example.com
REPRO_PASSWORD=
